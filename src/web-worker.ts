// import consts from "./data/consts";
// import RayCasting from "./data/rayCasting";
// import { PlayerState } from "./data/types";
// console.log('web worker is ready');
// onmessage = (e: { data: { imageData: ImageData, playerState: PlayerState, from: number, to: number } }) => {
//     const rayCasting = new RayCasting(e.data.imageData, e.data.playerState);
//     rayCasting.draw3D(e.data.from, e.data.to);
//     self.postMessage(e.data.imageData);
// }

// export {}